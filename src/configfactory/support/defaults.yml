server:
  host: 127.0.0.1
  port: 8080

cleansed:
  hidden: "api token key secret pass password signature"
  substitute: "***************"

database:
  url: "sqlite://:memory:"

backup:
  dir: configfactory_backup
  interval: 3600
  count: 20

environments:
  - alias: development
    name: Development

users:
  - username: admin
    password: admin
